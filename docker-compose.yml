version: "3.9"

services:
  clawlite:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import clawlite"]
      interval: 30s
      timeout: 10s
      retries: 3
